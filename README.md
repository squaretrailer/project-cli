# Project Tracker CLI

A simple command-line tool to manage users, projects, and tasks.  
Data is stored in JSON files. Uses `tabulate` for pretty tables and `colorama` for coloured output.

## Installation

```bash
pip install -r requirements.txt